<% provide(:title, "Sign in") %>
<h1>Create New Story</h1>
  <div>
	<%= form_for(@story) do |f| %>
		   <%= f.label :title %>
		   <%= f.text_field :title %>

		   <%= f.label :body %>
		   <%= f.text_field :body %>

	       <%= f.submit 'Create Story' %>
	    <% end -%>
  </div>
